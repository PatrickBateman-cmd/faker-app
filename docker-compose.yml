services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DUCKDB_PATH=/data/duckdb
    volumes:
      - duckdb_data:/data/duckdb

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  duckdb_data:
